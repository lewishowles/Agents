{
	"name": "human-friendly-cli",
	"description": "Use this skill when designing, reviewing, or extending CLI commands. Covers discoverable, restrained human-first CLIs that also work well for agents.",
	"when": "When designing, reviewing, or extending CLI commands",
	"triggers": [
		"CLI",
		"command line",
		"subcommand",
		"--help",
		"--json",
		"cli-style",
		"terminal output",
		"create command",
		"add command"
	],
	"do-not-use-when": [
		"Preparing a library-release",
		"Only running an existing CLI command",
		"Debugging command output without changing the CLI design",
		"Writing a shell script; use bash instead"
	],
	"filePatterns": [],
	"pathPatterns": [],
	"dependencies": ["writing"],
	"capabilities": {
		"promptTriggering": true,
		"fileTriggering": false
	}
}
