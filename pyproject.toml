[project]
name = "agents"
version = "0"
description = "Workspace root for independent agent-related scripts."
requires-python = ">=3.11"

[tool.ruff.format]
indent-style = "tab"

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]
