{
	"name": "guard-hcom-ack",
	"description": "Suppresses acknowledgement-only HCOM messages so team roles wait silently for actionable work or terminal receipts. Rewrites the send to a no-op on Claude; blocks it on Codex.",
	"runtime": "claude",
	"events": [
		{
			"event": "PreToolUse",
			"matcher": "Bash"
		}
	],
	"dependencies": ["jq"],
	"failureMode": "silent"
}
